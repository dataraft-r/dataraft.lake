% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/registry.R
\name{dr_register}
\alias{dr_register}
\title{Register a versioned definition}
\usage{
dr_register(lake, object)
}
\arguments{
\item{lake}{Connected lake.}

\item{object}{A contract, source, product, metric or pipeline definition.}
}
\value{
The definition, invisibly. Reusing a version with changed content
errors.
}
\description{
Register a versioned definition
}
\examples{
\dontshow{if (requireNamespace("duckdb", quietly = TRUE)) withAutoprint(\{ # examplesIf}
root <- tempfile("dataraft-example-")
config <- dr_lake_config(
  dr_registry_duckdb(file.path(root, "lake.db")),
  dr_storage_local(file.path(root, "data")),
  landing = file.path(root, "landing"), backend = "duckdb"
)
lake <- dataraft.lake::dr_connect_lake(config)
contract <- dataraft.core::dr_contract(
  "orders", version = "1.0.0", columns =
  c(order_id = "integer", amount = "numeric"), key = "order_id"
)
dr_register(lake, contract)
dr_registry(lake, "assets")
dataraft.lake::dr_disconnect_lake(lake)
unlink(root, recursive = TRUE)
\dontshow{\}) # examplesIf}
}
