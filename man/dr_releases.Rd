% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{dr_releases}
\alias{dr_releases}
\title{List published release history}
\usage{
dr_releases(lake, asset = NULL)
}
\arguments{
\item{lake}{Connected lake.}

\item{asset}{Optional asset ID.}
}
\value{
A tibble sorted newest first. Historical releases are retained.
}
\description{
List published release history
}
\examples{
\dontshow{if (requireNamespace("duckdb", quietly = TRUE)) withAutoprint(\{ # examplesIf}
root <- tempfile("dataraft-")
lake <- dataraft.lake::dr_connect_lake(dr_lake_config(dr_registry_duckdb(file.path(root, "lake.db")),
  dr_storage_local(file.path(root, "data")),
  landing = file.path(root, "landing"), backend = "duckdb"))
dr_releases(lake)
dataraft.lake::dr_disconnect_lake(lake)
unlink(root, recursive = TRUE)
\dontshow{\}) # examplesIf}
}
