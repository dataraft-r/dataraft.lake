% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/integrity.R
\name{dr_verify_releases}
\alias{dr_verify_releases}
\title{Verify stored releases against their publication fingerprints}
\usage{
dr_verify_releases(lake, release_ids = NULL)
}
\arguments{
\item{lake}{Connected lake.}
\item{release_ids}{Optional character vector of release IDs; NULL checks all.}
}
\value{A tibble with release ID, asset, status, expected and observed counts.}
\description{
Reads stored tables and compares their row counts and content fingerprints with
the registry. Row order does not affect the fingerprint. Existing releases
migrated from older registries are unverified. Missing tables, changed content
and inconsistent registry references are reported separately.
}
\details{
This detects accidental mutation, not an administrator who can rewrite both
data and registry. Hashes describe the DBI representation and are not promised
portable across driver or R serialization changes. Verification scans all
selected rows and retains one digest per row in memory. Use a maintenance window
to prevent direct clients changing tables during verification.
}
\examples{
if (FALSE) dr_verify_releases(lake)
}
