% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simple.R
\name{dr_open_lake}
\alias{dr_open_lake}
\alias{dr_close_lake}
\title{Open a local lake with sensible defaults}
\usage{
dr_open_lake(
  path = "dataraft",
  backend = NULL,
  read_only = FALSE,
  layers = NULL,
  install_extensions = TRUE
)

dr_close_lake(lake)
}
\arguments{
\item{path}{Local folder, created if needed. Defaults to \code{"dataraft"} in
the working directory.}

\item{backend}{Optional \code{"duckdb"} or \code{"ducklake"}. DuckLake requires its
DuckDB extension. Defaults to the saved choice, or DuckDB for a new folder.}

\item{read_only}{Open an existing lake without registry or data writes.}

\item{layers}{Layer names, optionally named by role. For a new folder the
default is \code{c("raw", "validated", "products")}. Omit on subsequent opens
to reuse saved layers. Explicit conflicting settings are rejected.}

\item{install_extensions}{Allow installation of required DuckDB extensions.
Use \code{FALSE} when the required extensions are already installed.}

\item{lake}{Connected lake to close.}
}
\value{
\code{dr_open_lake()} returns a connected \code{dr_lake}. \code{dr_close_lake()} invisibly
returns \code{TRUE}; it is an alias for \code{\link[=dr_disconnect_lake]{dr_disconnect_lake()}}.
}
\description{
Creates or reopens a self-contained local folder. DuckDB is the default and
needs no extension download or external service. Use \code{backend = "ducklake"}
for an actual DuckLake. Backend and ordered layers (including named roles)
are remembered in \code{dataraft.json}; reopening never silently switches them.
A new lake needs an empty or nonexistent folder. Existing lakes made with
custom remote or split-location configuration still open through
\code{\link[=dr_connect_lake]{dr_connect_lake()}}. Use \code{\link[=dr_lake_config]{dr_lake_config()}} for a connection-free definition.
}
\details{
Read-only opens never update the folder configuration.
}
\examples{
\dontshow{if (requireNamespace("duckdb", quietly = TRUE)) withAutoprint(\{ # examplesIf}
root <- tempfile("dataraft-")
lake <- dr_open_lake(root)
dr_write_data(lake, data.frame(id = 1:2), "orders")
dr_read_release(lake, "orders")
dr_close_lake(lake)
lake <- dr_open_lake(root) # reopens the same data
dr_read_release(lake, "orders")
dr_close_lake(lake)
unlink(root, recursive = TRUE)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=dr_write_data]{dr_write_data()}}, \code{\link[=dr_read_release]{dr_read_release()}}
}
