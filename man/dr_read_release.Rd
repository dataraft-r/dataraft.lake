% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simple.R
\name{dr_read_release}
\alias{dr_read_release}
\title{Read a published asset}
\usage{
dr_read_release(lake, name, release = NULL, lazy = FALSE)
}
\arguments{
\item{lake}{Connected lake.}

\item{name}{Published asset name.}

\item{release}{Optional release ID. Defaults to the latest release.}

\item{lazy}{Return a lazy database table instead of collecting all rows.}
}
\value{
A tibble, a lazy \code{tbl_sql} when \code{lazy = TRUE}, or a dm for a
model product. Models restore all member tables from the same manifest.
}
\description{
Returns the latest successfully published data as a tibble. Failed writes
leave that release intact. Set \code{lazy = TRUE} to filter or aggregate in the
database before collecting large data; keep the connection open while using
a lazy table. Use \code{release} to read an exact historical version.
}
\examples{
\dontshow{if (requireNamespace("duckdb", quietly = TRUE)) withAutoprint(\{ # examplesIf}
root <- tempfile("dataraft-")
lake <- dr_open_lake(root)
dr_write_data(lake, data.frame(id = 1:3), "orders")
dr_read_release(lake, "orders")
dr_read_release(lake, "orders", lazy = TRUE) |>
  dplyr::filter(id > 1) |>
  dplyr::collect()
dr_close_lake(lake)
unlink(root, recursive = TRUE)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=dr_write_data]{dr_write_data()}}, \code{\link[=dr_tbl]{dr_tbl()}}, \code{\link[=dr_releases]{dr_releases()}}
}
