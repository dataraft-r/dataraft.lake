% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipeline.R
\name{dr_interrupted}
\alias{dr_interrupted}
\title{Identify interrupted runs without changing metadata}
\usage{
dr_interrupted(lake, older_than_hours = 1)
}
\arguments{
\item{lake}{Connected lake.}

\item{older_than_hours}{Age after which a running job may need investigation.}
}
\value{
Runs still marked running. Never auto-cancels a possibly live writer.
}
\description{
Identify interrupted runs without changing metadata
}
\examples{
\dontshow{if (requireNamespace("duckdb", quietly = TRUE)) withAutoprint(\{ # examplesIf}
root <- tempfile("dataraft-example-")
config <- dr_lake_config(
  dr_registry_duckdb(file.path(root, "lake.db")),
  dr_storage_local(file.path(root, "data")),
  landing = file.path(root, "landing"), backend = "duckdb"
)
lake <- dataraft.lake::dr_connect_lake(config)
dr_interrupted(lake)
dataraft.lake::dr_disconnect_lake(lake)
unlink(root, recursive = TRUE)
\dontshow{\}) # examplesIf}
}
