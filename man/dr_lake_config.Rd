% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/setup.R
\name{dr_lake_config}
\alias{dr_lake_config}
\title{Define a lake without opening a connection}
\usage{
dr_lake_config(
  catalog = dr_registry_duckdb("metadata.ducklake"),
  storage = dr_storage_local("data"),
  layers = c("raw", "validated", "products"),
  landing = "landing",
  backend = c("ducklake", "duckdb"),
  install_extensions = TRUE,
  read_only = FALSE,
  path = NULL
)
}
\arguments{
\item{catalog}{Metadata configuration.}

\item{storage}{Data file storage configuration.}

\item{layers}{Ordered schema names.}

\item{landing}{Local immutable landing directory and S3 staging folder.}

\item{backend}{\code{"ducklake"} or local \code{"duckdb"}.}

\item{install_extensions}{Allow DuckDB to install required extensions.}

\item{read_only}{Attach existing storage read-only and skip creation writes.}

\item{path}{Optional local lake folder. Derives \code{metadata.duckdb}, \code{data}
and \code{landing} within that folder. Supply either \code{path} or explicit
\code{catalog}, \code{storage} and \code{landing}, not both. An explicit backend must
agree with a folder's saved backend. Explicit layers must match its saved
layers; omit them when reopening. Default layers are unchanged;
select \code{layers = c("raw", "staging", "core", "marts")} when needed.}
}
\value{
A connection-free \code{lake_config} specification.
}
\description{
This constructor validates configuration and resolves paths, but does not
create directories, install extensions, connect to databases or read secrets.
With \code{path}, it may read an existing \code{dataraft.json} to preserve the saved
backend and ordered layers, including named layer roles. It never creates
or writes files. A new shorthand lake defaults to
DuckDB; other calls retain the usual DuckLake default. The local layout and
backend marker are shared with \code{\link[=dr_open_lake]{dr_open_lake()}}. Unknown non-empty folders are
refused rather than interpreted as a new lake.
Pass its result to \code{\link[=dr_connect_lake]{dr_connect_lake()}} or \code{\link[=dr_target_lake]{dr_target_lake()}} when ready to execute.
}
\examples{
config <- dr_lake_config(backend = "duckdb")
print(config)
local <- dr_lake_config(path = file.path(tempdir(), "my-data-lake"))
print(local)
}
