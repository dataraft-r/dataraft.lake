% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ingestion.R
\name{dr_ingest}
\alias{dr_ingest}
\title{Check a delivery and publish an accepted raw release}
\usage{
dr_ingest(
  x,
  to = NULL,
  name = NULL,
  contract = NULL,
  quality = NULL,
  reader = NULL,
  execution = NULL,
  ...
)
}
\arguments{
\item{x}{Data frame, lazy table, local file path, zero-argument function,
source adapter or product. A product must have one ordinary source and no
transformations, lookups, target or catalogs. Its contract, quality rules,
name and code version are retained. Use \code{\link[dataraft.core:dr_publish]{dataraft.core::dr_publish()}} for transformed products.}

\item{to}{Local lake folder, connected lake or \code{\link[=dr_lake_config]{dr_lake_config()}} with a \code{raw}
layer. Defaults to a local \code{"dataraft"} folder, using the same configuration
and backend marker as \code{\link[=dr_open_lake]{dr_open_lake()}}.}

\item{name}{Optional asset name. Defaults to a file basename, source asset
or ID, or the data variable name; expressions use \code{"data"}.}

\item{contract}{Optional contract, named type vector or prototype list.}

\item{quality}{Optional input checks accepted by \code{\link[dataraft.core:dr_add_quality]{dataraft.core::dr_add_quality()}}.}

\item{reader}{Optional file reader. CSV, TSV, RDS and Excel have defaults.}

\item{execution}{Optional \code{\link[dataraft.core:dr_execution_config]{dataraft.core::dr_execution_config()}} defaults, overriding defaults
stored on a product. Its layer must be
\code{NULL} or \code{"raw"}; an explicit \code{to} overrides its destination.}

\item{...}{Named execution options: \code{stop_on_failure} (default \code{TRUE}),
\code{business_date}, \code{notify}, \code{code_version}, and \code{cache} (default \code{FALSE}).
Reusing a cached raw release requires an explicit \code{code_version}; live
reference checks always require \code{cache = FALSE}.}
}
\value{
A \code{dr_run_result}. Accepted results contain \code{asset}, \code{release_id},
\code{output_config}, and an \code{outputs} list with exact \code{database}, \code{schema}
and \code{table} identifiers. Failures can be returned with
\code{stop_on_failure = FALSE}; otherwise the error contains \code{condition$result}.
}
\description{
Reads a delivery once, keeps its original file in immutable landing, and
evaluates its contract and quality checks before writing any raw table.
Rejected input leaves previous accepted releases available. A successful
result points to an immutable physical table in the \code{raw} schema, suitable
for a dbt source binding. Landing is evidence of receipt, not acceptance.
}
\details{
Without a contract, the first accepted delivery establishes a structural
schema. Subsequent deliveries must match it. Required values, keys, owners
and freshness deadlines are optional and are never guessed. Additional
\code{quality} checks accept the same functions, formulas, named lists and
pointblank adapters as \code{\link[dataraft.core:dr_add_quality]{dataraft.core::dr_add_quality()}}. Business checks run at the input
gate; the stored candidate also undergoes structural validation.

Ordinary tables, dataset directories and non-file adapters are archived as
RDS snapshots. Single local files retain their original bytes. A snapshot
records the received R values, not an API's original transport response.
Ingestion collects lazy adapters into memory for this gate and snapshot.
For data exceeding available memory, use a lazy product and database target.
Connections opened from a configuration are closed before returning;
\code{\link[dataraft.core:dr_collect]{dataraft.core::dr_collect()}} can reopen the exact accepted release when needed.
}
\examples{
\dontshow{if (requireNamespace("duckdb", quietly = TRUE)) withAutoprint(\{ # examplesIf}
root <- tempfile("raw-ingestion-")
config <- dr_lake_config(path = root)
orders <- data.frame(id = 1:2, amount = c(25, 75))
accepted <- orders |> dr_ingest(to = config, quality = ~ amount >= 0)
dataraft.core::dr_collect(accepted)
accepted$outputs
unlink(root, recursive = TRUE)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[dataraft.adapters:dr_source_database]{dataraft.adapters::dr_source_database()}}, \code{\link[dataraft.core:dr_pointblank_checks]{dataraft.core::dr_pointblank_checks()}}, \code{\link[dataraft.core:dr_quality]{dataraft.core::dr_quality()}}, \code{\link[dataraft.core:dr_collect]{dataraft.core::dr_collect()}}
}
