% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/recovery.R
\name{dr_recover}
\alias{dr_recover}
\title{Recover explicitly selected abandoned runs and staging slots}
\usage{
dr_recover(
  lake,
  run_ids = character(),
  staging_assets = character(),
  dry_run = TRUE,
  writer_stopped = FALSE
)
}
\arguments{
\item{lake}{Connected lake.}

\item{run_ids}{Explicit run IDs to recover. Empty previews list interrupted
runs; execution never implicitly selects them.}

\item{staging_assets}{Explicit asset names whose staging slots to remove.}

\item{dry_run}{Preview only, the default.}

\item{writer_stopped}{Confirm that writers with unknown liveness were stopped
externally. Does not override a known live writer.}
}
\value{
A tibble identifying each selected run or staging slot, its writer
state and planned or completed action.
}
\description{
Preview first. A known live writer always blocks recovery. On Linux, the
recorded host, boot ID and process start distinguish a dead process from a
reused PID. For remote or unsupported hosts, recovery requires the
caller to stop the original writer and explicitly set \code{writer_stopped}.
Age alone never proves that a writer has stopped.
}
\details{
Selected running jobs become errors, retaining their inputs and quality
evidence. Selected data-frame staging slots are removed; immutable landing
deliveries and published tables are retained. Use \code{\link[=dr_cleanup]{dr_cleanup()}} separately
for abandoned candidate tables. Run recovery with one coordinated writer.
Database changes are transactional; staging removal happens afterwards and
is reported separately, so an incomplete filesystem cleanup can be retried.
}
\examples{
\dontshow{if (requireNamespace("duckdb", quietly = TRUE)) withAutoprint(\{ # examplesIf}
root <- tempfile("dataraft-")
lake <- dr_open_lake(root)
dr_recover(lake)
dr_close_lake(lake)
unlink(root, recursive = TRUE)
\dontshow{\}) # examplesIf}
}
